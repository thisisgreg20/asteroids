[project]
name = "asteroids"
version = "1.0.0"
description = "boot.dev asteroids python project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]

[tool.uv.workspace]
members = [
    "asteroids",
]
